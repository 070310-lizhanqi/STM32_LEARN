# 统一换行符处理，避免不同机器上出现整文件 diff
* text=auto eol=crlf

# 脚本类强制 LF，否则在 Linux/Git Bash 下会报 bad interpreter
*.sh   text eol=lf
*.py   text eol=lf

# 二进制文件不做换行转换
*.bin  binary
*.hex  binary
*.elf  binary
*.axf  binary
*.uvprojx binary
*.uvoptx  binary
*.pdf  binary
*.png  binary
*.jpg  binary
